
export const TopVideo = () => {
  const urlBase = "https://mintlify.s3.us-west-1.amazonaws.com/tonik-a57c11a2/videos/";
  const isLocalHost = window.location.hostname === "localhost" || window.location.hostname === "";
  const url = isLocalHost ? '/videos/extropic-wide' : urlBase + 'extropic-wide';
  
  return useMemo(() => (
    <div
        id="top-video"
        key="top-video"
      className="absolute w-screen pointer-events-none top-0 min-h-[400px] sm:ml-[0rem] lg:-ml-[9rem] left-1/2 -translate-x-1/2 z-0 flex items-center justify-center max-h-[720px] overflow-hidden opacity-60"
    >
      <video
        autoPlay
        loop
        muted
        playsInline
        className="w-full h-full object-cover pointer-events-none"

      >
        <source src={`${url}.mp4`} type="video/mp4" />
        <source src={`${url}.webm`} type="video/webm" />
      </video>
    </div>
  ), []);
};

